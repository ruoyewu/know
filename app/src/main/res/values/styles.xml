<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mountain_mist</item>
        <item name="colorPrimaryDark">@color/mountain_mist</item>
        <item name="colorAccent">@color/onyx</item>
        <item name="android:background">@color/romance</item>
    </style>

</resources>
